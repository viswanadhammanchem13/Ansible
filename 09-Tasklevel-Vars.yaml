- name: Task Lavel vars
  hosts: local
  connection: local
  vars:
    COURSE: Ansible
    DURATION: 10 HR
    TRAINER: Siva Kumar
  tasks:
  - name: Printing Course Information
    # Task Level Vars
    vars:
      COURSE: DevSecOPS with AWS
      DURATION: 120 HR
      TRAINER: Siva Kumar
    ansible.builtin.debug:
      msg: "Hello {{ COURSE }},Duration is:{{ DURATION }}, Trainer Name is:{{ TRAINER }}. " 

  - name: Printing Course Information Again.
    ansible.builtin.debug:
      msg: "Hello {{ COURSE }},Duration is:{{ DURATION }}, Trainer Name is:{{ TRAINER }}. " 