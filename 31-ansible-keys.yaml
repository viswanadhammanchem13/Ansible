- name: Connecting EC2 Instance with Ansible Keys
  hosts: Ansiblekeys
  become: yes
  tasks:
  - name: Installing Nginx
    ansible.builtin.dnf:
      name: nginx
      state: present

  - name: Starting Service
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes